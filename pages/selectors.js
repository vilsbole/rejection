// import { reduce, prop, map, pipe } from "ramda";

// export const getScore = (state = {}) => {
//   const points = {
//     Accepted: 1,
//     Rejected: 10,
//   };
//   const getStatus = prop("status");
//   const score = reduce((acc) => {}, 0);
//   return {};
// };
